# ⚠️ PREENCHA ISSO ANTES DE FAZER DEPLOY!

# URL do PostgreSQL (copie a "Internal Database URL" do Render)
DATABASE_URL=postgresql://postgres:SEU_PASSWORD@dpg-xxxxx.render.internal:5432/sculptx_db

# Senha do painel admin (MUDE ISSO PARA ALGO SEGURO!)
ADMIN_PASSWORD=01010924Clo#

# Porta (Render define automaticamente, deixa assim)
PORT=3000
